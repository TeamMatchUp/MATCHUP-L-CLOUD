

## Plan: Enable Auto-Confirm Email Signups

Use the `supabase--configure_auth` tool to set `auto_confirm_email: true` so test accounts can sign in immediately without email verification.

No code changes needed — this is a backend auth configuration change only.

